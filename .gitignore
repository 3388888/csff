node_modules/
dist/
*.log
.DS_Store
Thumbs.db

# Go build/test intermediates (source + the built .exe ARE committed so the app builds without Go)
native/**/*.test
*.tmp

# prebuilt 3D map geometry (generated by tools/build-geo.js; the app strips + caches
# it on demand anyway, so it's a local/packaging artifact, not source)
maps3d/
